[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_yaml = "0.9"
shared = { path = "../shared" }


tokio = { version = "1.40", features = ["full"] }
